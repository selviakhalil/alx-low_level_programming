#include <stdio.h>
#include "main.h"

/**
 * main - this is main function of every c
 * @argc: number of arguments
 * @argv: array of arguments
 * Return: array of arguments
 */
int main(int argc, char *argv[])
{
	(void)argc;

	printf("%s\n", argv[0]);
	return (0);
}
