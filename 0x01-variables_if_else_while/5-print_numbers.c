#include <stdio.h>
/**
 * main - main is first function of every program c
 * Return: always c
 */
int main(void)
{
	int i;

	for (i = 0 ; i < 10 ; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
